{
  "main": {
    "$": true,
    "set": {
      "subtotal": "$.order.subtotal",
      "taxRate": "$.order.taxRate"
    },
    "body": {
      "customerName": "$.customer.name",
      "total": "$pyrun= return round(subtotal * (1 + taxRate), 2)",
      "itemCount": "$pyrun= return len(_input['items'])",
      "itemSummary": "$pyrun=return ', '.join([i['name'] for i in _input['items']])"
    }
  }
}
