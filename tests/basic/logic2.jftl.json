{
  "main": {
    "customerName": "$.customer.name",
    "greeting": {
      "$": true,
      "case": [
        {
          "when": "$.customer.vip",
          "then": "$.vipMessage"
        },
        {
          "else": "$.standardMessage"
        }
      ]
    },
    "orderTotal": {
      "$": true,
      "check": "$.hasOrder",
      "out": "$.order.amount",
      "fallback": "$.noOrderMessage"
    },
    "itemNames": {
      "$": true,
      "foreach": {
        "in": "$.items",
        "out": "$.name"
      }
    }
  }
}