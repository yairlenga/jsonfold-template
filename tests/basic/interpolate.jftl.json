{
	"main": {
		"$": true,
		"set": {
			"count": 12,
			"prize": "bar",
			"name": "Joe Smith",
			"tax": "$py=2/3"
		},
		"out": {
			"interpolate": "Hello ${name}, you won ${count} ${prize}, tax=${tax}",
			"join_str": {
				"$": true,
				"out": [ "hello ", "$name", " you won ", "$count", " ", "$prize", ", ", "tax=", "$tax" ],
				"transform": "join"
			},
			"escape": "Hello ${name}, you won $${count} ${prize}, tax=${tax}",
			"all-escape": "Hello $${name}, you won $${count} $${prize}, tax=$${tax}"

		}
	}
}
