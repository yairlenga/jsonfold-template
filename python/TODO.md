1. Navigation shortcut enhancements:
- From the root
- from the template '@' ?
- from the current frame - may be '_'
- Via parent frames (not parent frame data)
- Data sets
  
2. Navigation with json pointer

3. Navigation thru objects methods ? should be explicitly enabled

4. Simple expression evaluate using python (unsafe, easy testing)

